package com.brotv.iptv.ui.theme

import androidx.compose.ui.graphics.Color

/** Single source of truth for the BRO TV cinematic black/gold identity. */
object BroTvColors {
    val Gold = Color(0xFFFFD21A)
    val GoldDeep = Color(0xFFD9A700)
    val GoldGlow = Color(0x66FFD21A)

    val BackgroundNight = Color(0xFF06111C)
    val SurfaceElevated = Color(0xFF0C1824)
    val SurfaceElevatedHigh = Color(0xFF111F2D)

    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFFD4D9E2)
    val TextDisabled = Color(0xFF858C99)

    val Divider = Color(0xFF304252)
    val Danger = Color(0xFFE5484D)
}
