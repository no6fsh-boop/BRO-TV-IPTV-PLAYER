name: BRO TV 2.0.3 Live TV UX Hotfix Gate

on:
  push:
    branches: [ brotv-2.0.3-live-ui-hotfix ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Download verified 2.0.3 source
        shell: bash
        run: |
          set -euo pipefail
          API='https://api.firestorage.ai/dev/file'
          SHARE='MvVHFYNLQ817'
          FILE_ID='01a0a3b1b98376b695a1f52346c66707'
          COOKIE="$RUNNER_TEMP/firestorage.cookies"
          SESSION=$(curl -sSf -c "$COOKIE" -b "$COOKIE" -X POST "$API/shares/$SHARE/cushion-sessions" -H 'Content-Type: application/json' --data '{"password":null}')
          CUSHION_ID=$(printf '%s' "$SESSION" | python3 -c 'import json,sys; print(json.load(sys.stdin)["cushionId"])')
          curl -sSfL -c "$COOKIE" -b "$COOKIE" "https://firestorage.ai/ja/go/$CUSHION_ID/" -o "$RUNNER_TEMP/cushion.html"
          DOWNLOAD_META=$(curl -sSf -c "$COOKIE" -b "$COOKIE" -X POST "$API/shares/$SHARE/files/$FILE_ID/download")
          DOWNLOAD_URL=$(printf '%s' "$DOWNLOAD_META" | python3 -c 'import json,sys; print(json.load(sys.stdin)["downloadUrl"])')
          curl -sSfL "$DOWNLOAD_URL" -o "$RUNNER_TEMP/source.zip"
          echo 'ab8b5ac4ed8a97207e2b9311ec581bc60d3001aafc786641b9281c4ec5822a90  '"$RUNNER_TEMP/source.zip" | sha256sum -c -
          mkdir -p "$GITHUB_WORKSPACE/project"
          unzip -q "$RUNNER_TEMP/source.zip" -d "$GITHUB_WORKSPACE/project"

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Verify live TV UX field fixes
        working-directory: project
        shell: bash
        run: |
          set -euo pipefail
          grep -n 'namespace = "com.brotv.player"' app/build.gradle.kts
          grep -n 'applicationId = "com.brotv.player"' app/build.gradle.kts
          grep -n 'versionCode = 23' app/build.gradle.kts
          grep -n 'versionName = "2.0.3"' app/build.gradle.kts
          grep -n 'android:id="@+id/liveControlsRow"' app/src/main/res/layout/activity_full_player.xml
          grep -n 'android:id="@+id/progressRow"' app/src/main/res/layout/activity_full_player.xml
          grep -n 'android:id="@+id/transportControlsRow"' app/src/main/res/layout/activity_full_player.xml
          grep -n 'LIVE_CHROME_TIMEOUT_MS = 4_000L' app/src/main/java/com/example/iptvplayer/ui/FullPlayerActivity.kt
          grep -n 'EXTRA_SURF_SCOPE' app/src/main/java/com/example/iptvplayer/ui/FullPlayerActivity.kt
          grep -n 'ChannelSurfScopePolicy.accepts' app/src/main/java/com/example/iptvplayer/ui/FullPlayerActivity.kt
          grep -n 'EXTRA_SURF_SCOPE, currentCategoryId' app/src/main/java/com/example/iptvplayer/ui/ChannelBrowseActivity.kt
          grep -n 'folder scope never accepts channels from another folder' app/src/test/java/com/example/iptvplayer/player/ChannelSurfScopePolicyTest.kt
          grep -n 'five seconds after first video frame never triggers black screen recovery' app/src/test/java/com/example/iptvplayer/player/LivePlaybackRecoveryPolicyTest.kt
          ! grep -R 'android.permission.RECORD_AUDIO' app/src/main/AndroidManifest.xml
          ! grep -R 'Log\.[divew](' app/src/main/java

      - name: Run all unit tests
        working-directory: project
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon testDebugUnitTest

      - name: Android release lint gate
        working-directory: project
        shell: bash
        run: |
          set -euo pipefail
          ./gradlew --no-daemon lintRelease
          REPORT='app/build/reports/lint-results-release.txt'
          test -s "$REPORT"
          for ID in WakelockTimeout HardcodedText TextFields Autofill CutPasteId SetTextI18n ContentDescription; do
            if grep -Fq "[$ID]" "$REPORT"; then
              echo "Blocked lint warning remains: $ID" >&2
              grep -F "[$ID]" "$REPORT" >&2 || true
              exit 1
            fi
          done

      - name: Build release APK and AAB
        working-directory: project
        run: ./gradlew --no-daemon assembleRelease bundleRelease

      - name: Prepare offline-signing package
        working-directory: project
        shell: bash
        run: |
          set -euo pipefail
          OUT="$GITHUB_WORKSPACE/final-build"
          mkdir -p "$OUT"
          APK=$(find app/build/outputs/apk/release -maxdepth 1 -type f -name '*unsigned*.apk' | head -1)
          AAB='app/build/outputs/bundle/release/app-release.aab'
          test -s "$APK"
          test -s "$AAB"
          BUILD_TOOLS=$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -1)
          "$BUILD_TOOLS/zipalign" -f 4 "$APK" "$OUT/BRO-TV-2.0.3-aligned-unsigned.apk"
          "$BUILD_TOOLS/zipalign" -c 4 "$OUT/BRO-TV-2.0.3-aligned-unsigned.apk"
          cp "$AAB" "$OUT/BRO-TV-2.0.3-unsigned.aab"
          cp "$BUILD_TOOLS/lib/apksigner.jar" "$OUT/apksigner.jar"
          cp "$RUNNER_TEMP/source.zip" "$OUT/BRO-TV-2.0.3-source.zip"
          cp app/build/reports/lint-results-release.txt "$OUT/"
          sha256sum "$OUT"/* > "$OUT/unsigned-sha256.txt"
          unzip -t "$OUT/BRO-TV-2.0.3-aligned-unsigned.apk" >/dev/null
          unzip -t "$OUT/BRO-TV-2.0.3-unsigned.aab" >/dev/null

      - uses: actions/upload-artifact@v4
        with:
          name: BRO-TV-2.0.3-LIVE-UX-HOTFIX
          path: final-build/
          retention-days: 7
